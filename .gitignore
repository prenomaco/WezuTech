node_modules
.next
.env
.env.local
*.tsbuildinfo
prisma/*.db
src/generated/prisma

# local visual QA
.shots/

# figma reference exports used for visual measurement only
.figma/

# database snapshots taken before a migration or reseed — these contain
# enquiry names and email addresses, so they stay out of the repository
.backups/
