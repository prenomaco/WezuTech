import type { Product, ProductMedia } from "@prisma/client";
import { saveProduct } from "@/app/admin/actions";
import { MediaUpload } from "@/components/media-upload";

export function AdminProductForm({ product, media }: { product?: Product; media?: ProductMedia | null }) {
  return <details className="admin-card" open={!product}><summary><strong>{product ? `Edit ${product.name}` : "Add product"}</strong></summary><form action={saveProduct} className="admin-form"><input type="hidden" name="id" value={product?.id ?? ""} /><label>Name<input required name="name" defaultValue={product?.name} /></label><label>Slug<input required name="slug" pattern="[a-z0-9]+(-[a-z0-9]+)*" defaultValue={product?.slug} /></label><label>Status<select name="status" defaultValue={product?.status ?? "DRAFT"}><option value="DRAFT">Draft</option><option value="PUBLISHED">Published</option><option value="ARCHIVED">Archived</option></select></label><label>Tagline<input name="tagline" defaultValue={product?.tagline ?? ""} /></label><label className="wide">Card description<textarea name="cardDescription" defaultValue={product?.cardDescription ?? ""} rows={3} /></label><label className="wide">Introduction<textarea name="introduction" defaultValue={product?.introduction ?? ""} rows={4} /></label><label>SEO title<input name="seoTitle" defaultValue={product?.seoTitle ?? ""} maxLength={70} /></label><label>SEO description<input name="seoDescription" defaultValue={product?.seoDescription ?? ""} maxLength={160} /></label><MediaUpload initialUrl={media?.url} /><div className="wide"><button className="button">Save product</button></div></form></details>;
}
